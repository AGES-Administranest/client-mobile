export type IconProps = {
  /** Rendered width/height in points. Defaults to 16 (the Figma icon size). */
  size?: number;
  /** Fill/stroke color. Defaults to white, since icons sit on the primary button. */
  color?: string;
};
